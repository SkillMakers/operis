server:
  port: 8083

spring:
  application:
    name: operis-user-profile-service
  config:
    import: optional:configserver:/
  cloud:
    config:
      discovery:
        service-id: operis-config-server
        enabled: true
